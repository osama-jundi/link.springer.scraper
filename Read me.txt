This is a python script that scrape the website https://link.springer.com/
you have to assign the first part of the url the part before the page number to url part one
and you have to assign the seconed part the part after the page number to url part two 
when you assign the seconed be carefull of (") you have to put(\") to make it work
than you enter the first page you wish to start scraping and than the one after the last you wish to scrap
after the python code scrape what you want 
it will create an exel sheet of 7 columns Title, year, journal, authores, link, doi, abstract
and it will save it to ("data.xlsx")